📘 What this code does
Loads a vector file (shapefile) obtained after classifying a satellite image.

Filters geographic features by the CLASS_NAME == "Class 1" attribute—for example, these could be bodies of water, fires, or vegetation.

Builds a map with the selected features using GeoPandas and Matplotlib.
- **fiona**

---



